# disertatie2025-api-client
1. ```
   python -m venv .venv
   ```
2. ```
   pip install -r requirements.txt
   ```

In order to build the new client package:
1. ```
   pip install build
   ```
2. ```
   python -m build
   ```